using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerHitRegisterBoxColliderScript : MonoBehaviour
{// declarations
    public WaterDropletScript wds;
    public PlayerScripts ps;
    // this stands for "this. trigger collider"
    public BoxCollider2D ttc;
    // water Droplet game object
    public GameObject wd;

    // Start is called before the first frame update
    void Start()
    {
        
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
   
}
